namespace CoffeeShop
{
    /// <summary>
    /// Defines the types of messages that can be displayed to the player.
    /// </summary>
    public enum MessageType
    {
        Info,
        Warning,
        Error
    }
}