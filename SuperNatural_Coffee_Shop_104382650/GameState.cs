namespace CoffeeShop
{
    /// <summary>
    /// Represents the different states of the game.
    /// </summary>
    public enum GameState
    {
        MainMenu,
        Playing,
        Paused,
        GameOver,
        GameComplete
    }
}